generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  image     String?
  role      String   @default("user")
  createdAt DateTime @default(now())
}

model Project {
  id          String   @id @default(cuid())
  title       String
  description String
  techStack   String[]
  githubUrl   String?
  liveUrl     String?
  featured    Boolean  @default(false)
  order       Int      @default(0)
  createdAt   DateTime @default(now())
}

model BlogPost {
  id          String    @id @default(cuid())
  slug        String    @unique
  title       String
  excerpt     String
  content     String?
  tags        String[]
  published   Boolean   @default(false)
  publishedAt DateTime?
  createdAt   DateTime  @default(now())
}

// Cached "Lately" items for the homepage strip (latest YouTube upload,
// current song, current book). Written by ingestion crons later; for now
// seeded/edited by hand.
model LatelyItem {
  id        String   @id @default(cuid())
  kind      String   @unique // "video" | "song" | "book"
  title     String
  subtitle  String?
  url       String?
  imageUrl  String?
  updatedAt DateTime @updatedAt
}

model ActivityLog {
  id        String   @id @default(cuid())
  date      DateTime
  category  String
  count     Int      @default(1)
  note      String?
  createdAt DateTime @default(now())

  @@unique([date, category])
}
