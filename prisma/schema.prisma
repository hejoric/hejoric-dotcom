generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

model User {
  id        String   @id @default(cuid())
  email     String   @unique
  name      String?
  image     String?
  role      String   @default("user")
  createdAt DateTime @default(now())
}

model Project {
  id          String   @id @default(cuid())
  title       String
  description String
  techStack   String[]
  githubUrl   String?
  liveUrl     String?
  featured    Boolean  @default(false)
  order       Int      @default(0)
  createdAt   DateTime @default(now())
}

model BlogPost {
  id          String    @id @default(cuid())
  slug        String    @unique
  title       String
  excerpt     String
  content     String?
  tags        String[]
  published   Boolean   @default(false)
  publishedAt DateTime?
  createdAt   DateTime  @default(now())
}

// Hand-logged activity for the non-code tracker categories (music, language,
// fitness, reading). Code is never stored here: it is fetched live from the
// GitHub contribution API in lib/github.ts, so there is one source of truth.
model ActivityLog {
  id        String   @id @default(cuid())
  date      DateTime
  category  String
  count     Int      @default(1)
  note      String?
  createdAt DateTime @default(now())

  @@unique([date, category])
}
